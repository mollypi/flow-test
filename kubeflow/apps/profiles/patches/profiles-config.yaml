apiVersion: v1
data:
  $patch: replace # We do not need some of the original fields.
  admin: molly.depew@zebra.com # {"$kpt-set":"email"}
  gcp-sa: kubeflow-test-user@gcp-dev-ops.iam.gserviceaccount.com # {"$kpt-set":"gcp-sa"}
kind: ConfigMap
metadata:
  name: config
